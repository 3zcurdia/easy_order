<%= form_with(model: merchant, url: builder_merchant_path(merchant)) do |f| %>
  <%= f.hidden_field :user_id %>
  <div class="field has-addons has-addons-centered">
    <div class="control has-icons-left has-icons-right">
      <%= f.text_field :name, class: 'input is-medium', placeholder: "El nombre de tu negocio" %>
      <span class="icon is-small is-left">
        <i class="fas fa-briefcase fa-xs"></i>
      </span>
      <span class="icon is-small is-right">
        <i class="fas fa-check fa-xs"></i>
      </span>
    </div>

    <div class="control">
      <%= f.submit 'Crea tu menú', class: 'button is-info is-medium' %>
    </div>
  </div>
<% end %>