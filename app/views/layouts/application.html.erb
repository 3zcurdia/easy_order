<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>MenuFacilito.com</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <nav class="navbar is-light" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
      <%= link_to user_root_path, class: 'navbar-item' do %>
        <p class="title is-4">
          <span class="icon"><i class="fas fa-utensils"></i></span> MenúFacilito
        </p>
      <% end %>
      <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false">
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </a>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            <%= link_to "Salir", destroy_user_session_path, class: 'button is-primary', method: :delete %>
          <% else %>
            <%= link_to 'Ingresar', new_session_path(:user), class: 'button' %>
            <%= link_to new_registration_path(:user), class: 'button is-primary' do %>
              <strong>Registrarse</strong>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
  <section class="section">
    <div class="container">
      <% if flash[:notice].present? %>
      <div class="notification is-primary is-light">
        <button class="delete"></button>
        <%= notice %>
      </div>
      <% end %>
      <% if flash[:alert].present? %>
      <div class="notification is-warning is-light">
        <button class="delete"></button>
        <%= alert %>
      </div>
      <% end %>
      <%= yield %>
    </div>
  </section>
  <script src="https://kit.fontawesome.com/238d584d83.js" crossorigin="anonymous"></script>
</body>

</html>
